# Webhook-router
Webhook Router &amp; Event Pipeline as a Service La idea es un sistema que recibe webhooks de servicios externos (GitHub, Stripe, cualquier HTTP POST), los procesa con lógica configurable, y los retransmite a destinos como Slack, email, o una base de datos — todo en arquitectura serverless orientada a eventos.
